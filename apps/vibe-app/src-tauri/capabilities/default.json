{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Vibe Remote shell.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
