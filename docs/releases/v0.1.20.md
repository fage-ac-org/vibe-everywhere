# v0.1.20

- Rework app conversation loading so reopening the app restores prior chat state behind a loading
  placeholder instead of briefly showing empty server, ACP, or model prompts.
- Remove duplicate conversation event cards, keep detailed event output collapsed by default, and
  render a clearer bilingual assistant in-progress placeholder while tasks are still running.
- Make chat sends feel immediate by showing local optimistic user and assistant turns before the
  refreshed conversation detail arrives from the relay.
- Tighten the app's conversation context controls and recent chat list density so project, ACP, and
  model switching stays available with less visual weight.
