## Highlights

- Reworked the control app around a clearer session-first primary workflow.
- Moved relay connection, device choice, session launch, and result review onto the same `Sessions` surface.
- Demoted deployment metadata and governance context into the secondary `Devices` view while keeping `Advanced` focused on terminal and preview tools.

## Included Iterations And Remediations

- Iteration 14: Session-First Primary Workflow Productization

## Operator Notes

- Web, Desktop, and Android clients now open into the session-first workflow model. Update any operator screenshots or onboarding notes that still reference a separate top-level `Connections` page.

## Validation

- `cargo fmt --all --check`
- `cargo check -p vibe-relay -p vibe-agent -p vibe-app`
- `cargo test --workspace --all-targets -- --nocapture`
- `cd apps/vibe-app && npm run build`
- `./scripts/dual-process-smoke.sh relay_polling`
- `./scripts/dual-process-smoke.sh overlay`
- `./scripts/render-release-notes.sh v0.1.7 >/dev/null`
