## Highlights

- Reissued the conversation-first remote coding release with a synchronized Rust workspace
  `Cargo.lock`, so `cargo check --locked`, CI verification, and release packaging run against the
  committed workspace state instead of trying to rewrite the lockfile on GitHub Actions.
- Carries forward the conversation-first remote coding workflow, durable relay-backed
  conversations, and ACP continuation improvements introduced in `v0.1.10`.

## Included Iterations And Remediations

- Iteration v5 / Iteration 15 tranche: Conversation-First Threaded AI Sessions
- Release remediation: synchronize workspace version and `Cargo.lock` for locked CI/release builds

## Operator Notes

- This release does not change relay or agent deployment inputs compared with `v0.1.10`; it fixes
  the published release pipeline so the same code can be built and published reproducibly.
- Published CLI assets continue to use the operator-facing naming scheme with the tag in the
  filename, for example `vibe-everywhere-cli-v0.1.11-x86_64-unknown-linux-musl.tar.gz`.

## Validation

- `cargo check --locked -p vibe-relay -p vibe-agent -p vibe-app`
- `cargo test --locked --workspace --all-targets -- --nocapture`
- `cd apps/vibe-app && npm run build`
- `./scripts/render-release-notes.sh v0.1.11 >/dev/null`
