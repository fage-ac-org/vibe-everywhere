[package]
name = "vibe-wire"
version = "0.0.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
cuid2 = "0.1.4"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
